<!DOCTYPE html>
<html lang="ja">
  <head>
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js"></script>
  </head>
  <body>
  <% if current_user.user_goals.exists?(finish: false)  %>
    <h1>目標確認</h1>
    
      <TABLE class="table goal-table">
       <TR>
         <TH class="goal-status">状態</TH>
         <TH class="goal-string">目標</TH>
         <TH class="goal-money">金額</TH>
         <TH class="goal-tweet" />
       </TR>
        <% @goals.each do |goal| %>
	      <TR>
          <% if goal.finish? %>
            <TD class="goal-achieved">達成</TD>
            <TD><%=goal.buttonStr.to_s %></TD>
            <TD><%=goal.goalMoney.to_i %></TD>
            <TD class="goal-tweet" />

          <% else %>
            <TD class="goal-trying">未達成</TD>
            <TD><%=goal.buttonStr.to_s %></TD>
            <TD><%=goal.goalMoney.to_i %></TD>
            <TD class="goal-tweet"/>
<TD><a href="/edit" class="large-button large-button-a" >変更</a></TD>
<TD><a href="cdhttps://twitter.com/share" class="twitter-share-button" data-text="<%=goal.buttonStr.to_s %>に使いたいので<%=goal.goalMoney.to_i %>円貯めます！ #したつもり貯金 #やるで #aiit_enpit"  data-url="https://goo.gl/D6RWc0"  data-size="small">Tweet</a></TD>
<script>
if (typeof twttr === 'undefined') {
!function(d,s,id){var js,fjs=d.getElementsByTagName(s)[0],p=/^http:/.test(d.location)?'http':'https';if(!d.getElementById(id)){js=d.createElement(s);js.id=id;js.src=p+'://platform.twitter.com/widgets.js';fjs.parentNode.insertBefore(js,fjs);}}(document, 'script', 'twitter-wjs');
} else {
 twttr.widgets.load();
}
</script>

            </TD>

          <% end %>
           </TR>
        <% end %>
      </TABLE>
  <% else %>
    <h1>目標設定</h1>
        <%= form_tag('/rgtr-goal-money', name: "my_form", method: :post) do %>
            <p> 目標：
            <input required type="text" name="text_goal" maxlength='20'></p>
            <p> 目標金額：
            <%= number_field_tag 'money', nil, min: 1, max: 50, step: 1, required: true %>
            000円</p>
            <input type="submit" tabIndex="0" value = 設定する！！ class="large-button">
        <% end %> 
  <% end %>  
   <h1>したつもりにするもの</h1>
        <% if current_user.endurances.exists? %>
        <h2><%= @curent_endurance_text  %></h2>
　　　　<% else %>
      <h2>現在の設定：コーヒーを飲む</h2>
        <% end %>
        <%= form_tag('/endurance', name: "my_form", method: :post) do %>
            <p> 設定内容：
            <input required type="text" name="endurance" maxlength='20'></p>
            <input type="submit" tabIndex="0" value = 設定する！！ class="large-button">
        <% end %>


 
  </body>
</html>
