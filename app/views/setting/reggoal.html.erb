<!DOCTYPE html>
<html lang="ja">
  <head>
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js"></script>
  </head>
  <body>
  <% if current_user.user_goals.exists?(finish: false)  %>
    <h1>目標確認</h1>
      <TABLE border="1" class="table table-borderd goal-table">
       <TR>
         <TH class="goal-status">状態</TH>
         <TH class="goal-string">目標</TH>
         <TH class="goal-money">金額</TH>
       </TR>
        <% @goals.each do |goal| %>
	      <TR>
          <% if goal.finish? %>
            <TD class="goal-achieved">達成</TD>
          <% else %>
            <TD class="goal-trying">未達成</TD>
          <% end %>
            <TD><%=goal.buttonStr.to_s %></TD>
            <TD><%=goal.goalMoney.to_s %></TD>
        </TR>  
        <% end %>
      </TABLE>
  <% else %>
    <h1>目標設定</h1>
        <%= form_tag('/regGoalMoney', name: "myForm", method: :post) do %>
            <p> 目標：
            <input required type="text" name="txtGoal" maxlength='20'></p>
            <p> 目標金額：
            <%= number_field_tag 'money', nil, min: 1, max: 50, step: 1, required: true %>
            000円</p>
            <input type="submit" tabIndex="0" value = 設定する！！ class="large-button">
        <% end %> 
  <% end %>   
  </body>
</html>
